# 同じPCでホストと参加者をデバッグ

リポジトリのルートで実行します。

```powershell
powershell -ExecutionPolicy Bypass -File .\Start-TwoClients.ps1
```

Debugビルド後、ローカルサーバーと2つのKuizを起動します。左がDebugHost、右がDebugGuestで、同じロビーに自動接続します。ウィンドウタイトルには役割とPIDを表示します。画面サイズによってはウィンドウを手動で調整してください。

設定、名前、戦績、問題履歴、ログは起動ごとに `%LOCALAPPDATA%\Kuiz-Debug\<session>\host` と `guest` に分離します。通常利用のデータは使いません。問題はローカルの3問で、Render/Neonへの書き込みはありません。

Visual Studioで「デバッグ → プロセスにアタッチ」から、表示されたPIDのKuiz.exeを選択してください。ホストと参加者にそれぞれアタッチできます。両方のクライアントを閉じるとスクリプトがサーバーを終了します。実行中は起動したPowerShellも開いておいてください。

## 操作確認

1. 両画面に同じロビーコードと2人の名前があることを確認。
2. ホストでゲーム開始。両画面でカウントダウン、同じ問題が出ることを確認。
3. 参加側で早押し。ホストに回答者が表示され、参加側だけ回答入力になることを確認。
4. 正解・不正解・時間切れを試し、得点／ミス数、次問への遷移、終了結果を比較。
5. 退出してホストの参加者一覧が更新されることを確認。

テスト問題の答えは `2`、`東京`、`cat`（出題順はシャッフルされます）。

## 通信の自動確認

2画面デバッグを終了してから実行します（同じ5187番ポートを使います）。

```powershell
dotnet run --project Tools/MultiplayerDebug/MultiplayerDebug.csproj
```

本番と同じGameHub/LobbyServiceを使い、ロビー参加、状態更新、早押し、回答、判定、次問、終了、ホスト権限、退出の通信を検証します。UIの描画や操作の正しさは上の手動手順で別途確認します。

WPF側の回帰確認は `Start-TwoClients.ps1 -Smoke` で実行できます。自動でゲーム開始し、双方の回答待ちモーダル状態、問題枯渇時の終了通知、両画面の勝者・2点の結果を検証し、セッションフォルダーの `regression.log` に記録します。回答や得点はテスト用に設定するため、キーボード操作・回答判定そのものは手動確認してください。
